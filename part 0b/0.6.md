sequenceDiagram
    participant user
    participant browser
    participant server

    user->>browser: Write text and click the Save button
    
    Note right of browser: The JS code adds the new note to the list locally and rerenders the notes on the page

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 Created (JSON: {"message":"note created"})
    deactivate server